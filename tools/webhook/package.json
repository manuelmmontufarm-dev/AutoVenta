{
  "name": "wa-webhook",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Webhook local para RECIBIR mensajes de la WhatsApp Cloud API (handshake de verificación + firma HMAC + log de mensajes entrantes)",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
